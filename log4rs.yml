refresh_rate: 30 seconds

appenders:
  stdout:
    kind: console

root:
  level: info
  # level: warn
  appenders:
    - stdout

loggers:
  strategy::strategy:
    # level: debug
    level: info

  # strategy::indicator:
  #   level: trace
